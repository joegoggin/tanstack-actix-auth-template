//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    printWidth: 80,
    tabWidth: 4,
    semi: true,
    singleQuote: false,
    jsxSingleQuote: false,
};
export default config;
